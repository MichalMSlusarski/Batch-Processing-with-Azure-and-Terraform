CREATE TABLE Users (
    user_id INT PRIMARY KEY,
    age INT,
    gender BOOL,
    -- Add other columns for city, state, country, preferred_genres, etc.
    -- Example: city VARCHAR(50), state VARCHAR(50), country VARCHAR(50), preferred_genres TEXT,
    -- ...
);
